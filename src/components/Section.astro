---
interface Props { title?: string; class?: string; }
const { title, class: cls = "" } = Astro.props;
---
<section class={`mx-auto max-w-5xl px-6 py-16 ${cls}`}>
  {title && <h2 class="font-display text-3xl font-bold text-brand">{title}</h2>}
  <slot />
</section>
